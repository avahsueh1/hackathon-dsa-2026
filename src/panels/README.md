Drop panel files here. See ../../docs/ADDING_A_PANEL.md.

  *.html  inlined into the page before the sources footer
  *.js    inlined into the app script, with all data in scope
  *.css   inlined into the stylesheet

Inlined in filename order, so prefix with a number nobody else is using
(20-, 30-, 40-). Two new files merge cleanly; edits to index.html do not.
